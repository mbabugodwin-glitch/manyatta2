# Quick Start: Using the New Design System

## 🎨 Design Tokens

### Import Tokens
```tsx
import { COLORS, TYPOGRAPHY, SPACING, SHADOWS, TRANSITIONS } from '../tokens';
```

### Common Color Usage
```tsx
// Primary color (main brand)
backgroundColor: COLORS.primary // #d4492f

// Dark text (headings)
color: COLORS.dark // #1a1a1a

// Light backgrounds
backgroundColor: COLORS.gray[50] // #f9fafb
